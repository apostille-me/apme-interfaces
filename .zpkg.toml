[package]
org = "apostille-me"
name = "apme-interfaces"
version = "0.1.0"
description = "Canonical Rust, JSON Schema, OpenAPI, AsyncAPI, and PostgreSQL contracts for Apostille Me"
license = "MIT"
language = "universal"

[package.repository]
vcs = "git"
url = "https://github.com/apostille-me/apme-interfaces"

[publish]
include_readme = true
tag_format = "v{version}"
exclude = [".env", ".env.*", "node_modules/**", "zed_modules/**", "target/**", "dist/**", "*.log"]

[targets.repository]
dir = "."

[install]
dir = "zed_modules"
adapter = "none"
